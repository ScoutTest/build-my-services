name: Test Success

on:
  workflow_dispatch:

jobs:
  success-job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo success
        run: echo "Success ✅"

      - name: Notify Scoutflo AutoCD
        run: |
          curl -X POST https://api.app-server-pp.scoutflo.com/v1/autocd/trigger \
            -H "Authorization: Bearer ${{ secrets.SCOUTFLO_WEBHOOK_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{
              "provider": "github",
              "service_name": "autocd-service-1",
              "cloud_connection_id": "cloud-credentials-3679c6df-9435-446d-91e5-7a743c467034",
              "registry": "AWS_ECR",
              "image_repo": "welcome-scoutflo",
              "image_tag": "${{ github.sha }}",
              "image_digest": "sha256:REPLACE_ME",
              "repo": "${{ github.repository }}",
              "branch": "${{ github.ref_name }}",
              "commit": "${{ github.sha }}"
            }'
